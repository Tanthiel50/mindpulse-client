import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    black: '#000',
    white: '#fff',
    purplish: '#AE8ABE',
    bluish: '#8390C8',
    background: '#121212',
  },
});

export default theme;